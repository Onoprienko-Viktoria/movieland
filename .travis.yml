language: java
jdk:
  - openjdk17

addons:
  sonarcloud:
    organization: "onoprienko-viktoria"
    token:
      secure: ${SONAR_TOKEN}

script:
  - mvn clean verify sonar:sonar -Pcoverage -Dsonar.projectKey=Onoprienko-Viktoria_movieland